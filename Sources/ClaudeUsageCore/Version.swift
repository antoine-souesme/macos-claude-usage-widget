/// Source unique du numéro de version.
///
/// Le script de construction le recopie dans l'`Info.plist` du bundle, et le
/// workflow de release vérifie que le tag poussé correspond à cette valeur.
public let appVersion = "1.0.4"
